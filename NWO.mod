name = "East vs West 0.1" # name of your mod, shown in launcher
path = "mod/EVW" # location of your mod files
user_dir = "EvW" # optional. creates a sub directory where settings/map cache/savegames etc can be safely written for your mod and not get mixed up  
